v##############################
# Application
##############################
NODE_ENV=development
PORT=6001

##############################
# Database
##############################
DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=noorai
DB_ENTITIES=dist/modules/**/*.model{.ts,.js}
DB_MIGRATIONS=dist/database/migrations/*{.ts,.js}
DB_SSL=false

##############################
# Redis / Bull Queue
##############################
REDIS_HOST=localhost
REDIS_PORT=6379

##############################
# Auth / JWT
##############################
JWT_SECRET=change_me
JWT_TIMEFRAME=3d
GOOGLE_CLIENT_ID=
APPLE_CLIENT_ID=

##############################
# SMTP Transport (primary)
##############################
MAIL_MAILER=smtp
MAIL_ENCRYPTION=TLS
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_username
SMTP_PASS=your_password
SMTP_FROM="DeenAI <no-reply@deenai.com>"

##############################
# Alternate MAIL_* variable names (optional)
##############################
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USERNAME=your_username
MAIL_PASSWORD=your_password
MAIL_FROM_NAME=DeenAI
MAIL_FROM_ADDRESS=no-reply@deenai.com

##############################
# Optional Resend compatibility
##############################
RESEND_API_KEY=

##############################
# Optional legacy domain key used in some templates
##############################
DOMAIN="DeenAI <support@deenai.com>"
